#include "main.h"
/**
 * *_strcat - concatenates two strings
 * @dest: pointer destination
 * @src: pointer source
 * Return: void
 */
char *_strcat(char *s1, char *s2)
{
int j = -1, i;
for (i = 0; s1[i] != '\0'; i++)
;
do 
{
j++;
dest[i] = src[a];
i++;
} 
while (s2[j] != '\0')
{
return (s1);
}
}
